---
import VariantLayout from "../../../layouts/VariantLayout.astro";
import VariantPage from "../../../components/variants/VariantPage.astro";

const variant = "field" as const;
---

<VariantLayout
  variant={variant}
  current="home"
  title="Field — Christian Staresina"
  description="Field mockup: olive utility portfolio layout with dark mode."
>
  <VariantPage variant={variant} page="home" />
</VariantLayout>
