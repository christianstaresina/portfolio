---
import VariantLayout from "../../../layouts/VariantLayout.astro";
import VariantPage from "../../../components/variants/VariantPage.astro";

const variant = "field" as const;
---

<VariantLayout
  variant={variant}
  current="resume"
  title="Résumé · Field — Christian Staresina"
  description="Field mockup résumé page."
>
  <VariantPage variant={variant} page="resume" />
</VariantLayout>
