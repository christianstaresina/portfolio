---
import VariantLayout from "../../../layouts/VariantLayout.astro";
import VariantPage from "../../../components/variants/VariantPage.astro";

const variant = "signal" as const;
---

<VariantLayout
  variant={variant}
  current="resume"
  title="Resume · Signal — Christian Staresina"
  description="Signal mockup resume page."
>
  <VariantPage variant={variant} page="resume" />
</VariantLayout>
